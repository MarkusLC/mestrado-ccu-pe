# Dados brutos do DATASUS e do IBGE — cache reproduzível, ~240 MB.
# Regenerar com `python3 pipeline.py`.
data/bruto/

# Derivados grandes: o painel é reproduzível a partir dos brutos. O que vai
# versionado é o agregado que alimenta o dashboard (data/dashboard.json).
data/painel_ccu_pe.csv
data/painel_ccu_pe.json

# Microdados do SISCAN filtrados para PE — centenas de MB, reproduzíveis com
# `python3 baixa_microdados.py`.
data/microdados/

# Python
__pycache__/
*.pyc
venv/
env/

# R
.Rhistory
.RData
.Rproj.user/
*.Rproj
renv/
.renvignore

# OS e editores
.DS_Store
.env
.env.local
.vscode/
.idea/
*.swp
*.swo

# Geral
.cache/
tmp/
